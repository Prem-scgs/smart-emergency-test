/**
 * Public API ของ admin users widget
 *
 * ตอนนี้เป็นหน้าเตรียมไว้สำหรับ user management จริงในอนาคต โดย route shell
 * import ผ่าน widget boundary นี้.
 */
export { default as UsersPage } from './ui/users-page'
